## 1. Pitch Deck — Core Hypothesis & Evidence Slide

- [x] 1.1 Rewrite briefing.html's Proposal slide 核心假說 text to state both understanding what the dose is for and confirming it was taken; verify by reading the rendered slide against the pitch-deck-narrative spec's "Core hypothesis covers comprehension and confirmation" scenario. (Wording tightened from "recognize which dose is due" to "understand what it's for" to stay consistent with the two-layer recognition insight — see spec note.)
- [x] 1.2 Add a new evidence slide immediately after the Proposal slide and before the Persona slides, with 4 stat cards in two labeled rows — Row 1 "這不是個案" (32.1%~35.4% polypharmacy prevalence + 19.6% non-adherence), Row 2 "而且常常是「看不懂」造成的" (43.3% label comprehension + 40.0% can't name current medication) — each attributed only to its abbreviated public source (never to poc.md itself); reused the existing `.bar`/`.card` grammar plus new `.evidence-*` classes for the big-stat numeral treatment. navdots/counter update automatically (JS reads `[data-slide]`); fixed the cover slide's hardcoded `data-target` indices and added a "資料佐證" cover-step link.

## 2. Pitch Deck — Insights & Decisions Content (wording)

- [x] 2.1 Remove Q3 ("測試後的迭代邏輯", currently "暫無") from the Insights & Decisions content; verify only Q1 and Q2 sub-points remain.
- [x] 2.2 Rewrite Q1's language-differentiation point to add the census primary-language statistic (65 歲以上 65.9% 主要用台語 vs. 28.5% 主要用國語) alongside the existing family-observation account, keeping it clearly distinct from the separate literacy-rate statistic; verify against the "Language-differentiation insight cites the census statistic" scenario.
- [x] 2.3 Rewrite Q2's first point ("不需要記錄吃了什麼藥" → "辨識不是比對藥品，是拆解「看不懂」背後的兩個不同問題") into the two-layer breakdown — label-comprehension already resolved by family pre-sorting, vs. purpose-comprehension that still isn't — as two separate `.qa-point-desc` paragraphs, with no citation naming poc.md; verify against the "Recognition-scope slide separates the two recognition problems" scenario. (Written as two paragraphs within the original combined slide; task 3 below moves it to its own dedicated slide, content unchanged.)

## 3. Pitch Deck — Insights & Decisions Structure (split + relocate)

Per design.md Decisions 8 and 9: replace the single combined Insights & Decisions slide with 5 single-topic slides, each tagged with its rubric question (Q1/Q2) plus the original question text in small print, placed immediately after the Evidence slide and before the Persona slides. Order: 3 live-narrated slides first, then 2 backup slides.

- [x] 3.1 Extracted Q1's language-differentiation point (task 2.2's content) into its own slide, tagged "洞察 Q1 · 研究與探索的洞察"; this is the 1st live-narrated slide.
- [x] 3.2 Extracted Q2's recognition-scope point (task 2.3's content) into its own slide, tagged "洞察 Q2 · 核心維度的深化"; this is the 2nd live-narrated slide.
- [x] 3.3 Extracted Q1's existing "遊戲化獎勵機制" point into its own slide, tagged "洞察 Q1 · 研究與探索的洞察"; this is the 3rd live-narrated slide (content unchanged from the existing QA point). Matches the "Family-photo insight slide explains the gamification rejection" scenario.
- [x] 3.4 Extracted Q2's "家屬端只跳異常狀況" point into its own backup slide, tagged "洞察 Q2 · 核心維度的深化", placed immediately after the 3 live slides.
- [x] 3.5 Extracted Q2's "沒有行事曆，只提醒最近一次的活動" point into its own backup slide, tagged "洞察 Q2 · 核心維度的深化", placed immediately after 3.4.
- [x] 3.6 Removed the old combined Insights & Decisions slide (was the deck's last slide); the 5 new slides (3.1–3.5) now sit as one contiguous block immediately after the Evidence slide, before the Persona slides. Each slide keeps the full original rubric question as small italic print (`.insight-question`) below its eyebrow tag, per design.md Decision 8's traceability requirement. `grep -c 'data-slide'` confirms 15 matches (14 real slides + 1 in the JS selector string) — matches the expected 14-slide total.
- [x] 3.7 Fixed the counter's initial fallback text to "01 / 14" and recomputed all 8 cover `data-target` indices for the new order: 提案=1, 資料佐證=2, 洞察=3 (new, points at the first of the 5 insight slides), Persona=8 (was 3), JTBD=10 (was 5), Design Goal=11 (was 6), Design Guide=12 (was 7), IA 架構=13 (was 8).
- [x] 3.8 Verified via `grep -n 'data-target'` that the cover-steps block is the *only* place in briefing.html referencing a slide index — Persona/JTBD/Design Goal/Design Guide/IA/the JS itself contain no other hardcoded position dependency, so nothing else needed fixing.

## 4. Prototype — Recognition Accent Moves to the Icon Badge (revises tasks 3–5 below from the earlier session)

Per design.md Decision 2's revision: the recognition accent recolors the medication icon badge itself instead of a separate dot, because the adjacent status tag already fully and independently triple-codes status — the badge's color was a redundant second copy of it, not the sole carrier.

- [x] 4.1 In prototype/styles.css, removed `.accent-dot` / `.accent-dot--sun` / `--pine` / `--earth`; added `.card__icon--earth` (new tint, following the existing `--warm`/`--muted`/`--danger` pattern) alongside the existing `.card__icon--warm` (sun) and `.card__icon--muted` (pine), which are reused as-is for the accent.
- [x] 4.2 prototype/01-today.html: removed the `.accent-dot` markup next to the caption; confirmed the badge already uses `.card__icon--warm` (sun) for the morning demo data, so no class change was needed there — caption renders correctly without the dot.
- [x] 4.3 prototype/03-medlist.html: removed the `.accent-dot` markup from each `.timeslot__caption`; changed each row's `.card__icon` class from its status-linked class to its accent class (row1 morning `--success`→`--warm` sun, row2 noon stayed `--muted` pine, row3 evening `--muted`→`--earth`); status tags (吃過了/可以吃了/尚未到時間, each with its own color+icon+text) are untouched and still fully convey status independently.
- [x] 4.4 prototype/10-manage-meds.html: removed the `.accent-dot` markup from each `.manage-list-row__caption`; changed each row's `.card__icon` class to its accent class (morning stays `--warm` sun, noon `--warm`→`--muted` pine, evening `--warm`→`--earth`).
- [x] 4.5 prototype/11-edit-med.html: confirmed no change needed — its `.accent-swatch` circular color-picker buttons are a distinct, legitimate UI pattern, not the same slot as the badge accent.

## 5. Documentation

- [x] 5.1 Updated PRODUCT.md's Information Architecture (screens 1, 3, 10, 11) and Evidence on Hand sections to describe the purpose-caption/accent fields and cite poc.md as the evidence source, with an explicit internal-only/no-filename-in-pitch caveat; verified the description matches what the updated screens actually show. (Still accurate after the dot-to-badge revision — PRODUCT.md describes the fields, not their exact visual treatment.)

## 6. Pitch Deck — Visual Grouping (Evidence & Proposal slides)

- [x] 6.1 Added `.evidence-group` wrapping each group's `.bar` + `.evidence-grid` pair (bar flush to the container's top edge via `border-radius:0` + `overflow:hidden` on the group; grid gets `clamp(14px,2vw,18px)` padding). Group background reuses the previously-unused `--primary-tint` token (no new color introduced); matches design.md Decision 7's Evidence-slide description.
- [x] 6.1a Follow-up from viewing the rendered slide: user reported the stacked-groups layout left too much whitespace per group and felt unfocused. Changed `.evidence-blocks` to a 2-column grid so the two groups sit side by side (collapsing to 1 column under 760px), and each group's own `.evidence-grid` from 2 columns to 1 (its 2 cards now stack vertically inside the half-width group) — same 4 stat cards, denser layout. See design.md Decision 4's updated "Concrete layout" note.
- [x] 6.1b Second follow-up: user reported the stacked cards within each half-width group still left wide near-empty cards. Reverted `.evidence-grid` to 2 columns (its original setting) so each group's 2 cards sit side by side under its own bar; combined with `.evidence-blocks` already being 2 columns, all 4 cards now render as one horizontal row (two 2-card pairs, each under its own bar). Added a 480px breakpoint so the within-group pair still collapses to 1 column on phone-width screens (the existing 760px breakpoint still collapses the two groups themselves).
- [x] 6.1c Third follow-up: user asked for an actual 2×2 grid with bigger numbers/text. Changed `.evidence-blocks` back to a vertical stack (one group per row, each group's own 2 cards side by side within it) — group 1's pair is row 1, group 2's pair is row 2, giving a literal 2-column/2-row card arrangement. Increased `evidence-stat` to `clamp(44px,6.5vw,72px)` (was `clamp(32px,4vw,46px)`), `evidence-label` to `clamp(19px,1.9vw,24px)` (was `clamp(16px,1.3vw,18px)`), `evidence-source` to 15px (was 13px), and bumped card/group padding and gaps to match — bigger content now fills the bigger cards, addressing the earlier whitespace complaints at the source instead of by rearranging. Added `flex:1` + `justify-content:space-evenly` to `.evidence-blocks` (same technique as the Proposal sheet, task 6.2a) so the two rows fill the slide's height evenly. Dropped the now-unneeded 760px breakpoint (groups are always full-width/stacked now); kept a 600px breakpoint collapsing each group's 2 cards to 1 column on phone widths.
- [x] 6.1d Fourth follow-up: removed `.evidence-group`'s tinted background and the bars' black fill. `.evidence-group .bar` now overrides the shared `.bar` class (`background:transparent; color:var(--ink); text-align:left; padding:0; margin-bottom:8px`), scoped so the base `.bar` class used elsewhere (e.g. JTBD) keeps its normal filled look. The 4 stat cards' own white `.card` background/shadow/border is untouched.
- [x] 6.1e Fifth follow-up: removed `.evidence-group .evidence-grid`'s `clamp(18px,2.6vw,28px)` padding entirely — it existed to inset cards from the group's tinted background, which is now gone, and was leaving the cards indented relative to the flush-left bar text. Bar and cards now share the same left edge.
- [x] 6.2 Added a `.proposal-sheet` frame (white card, shadow, generous padding) wrapping `.proposal-meta` and all six numbered proposal items; removed the individual `.card` class from all six `.proposal-card` items so they read as rows inside one document, with a thin top border + spacing between each of the sheet's 4 direct children (meta, two-col row, core row, three-col row) via `.proposal-sheet > * + *`; the core-hypothesis row keeps its own accent-tinted background (now with explicit padding/border-radius since it lost the shared `.card` styling) as the one emphasized element — checked it still reads clearly (accent-tint against the sheet's white background has more contrast than it had against the page's oat background before, so the Risk noted in design.md did not materialize).
- [x] 6.2a Follow-up: user reported the 6 rows were still packed at the top with a lot of dead space below on the rendered slide, asked for even distribution. Made `.proposal-sheet` `flex:1` + `display:flex;flex-direction:column;justify-content:space-evenly` so it fills the slide's remaining height and spreads its 4 rows evenly across it; dropped the divider rule's own `margin-top` in favor of the flex `gap`. See design.md Decision 7's Proposal-slide follow-up note.
- [x] 6.3 Removed the cover slide's `.cover-steps` section-jump navigation entirely, per user request, rather than continuing to re-derive its `data-target` indices on every slide-count change: deleted the HTML button block, the `.cover-steps`/`.cover-step` CSS, and the JS listener wiring `data-target` to `goTo()`. `prevBtn`/`nextBtn`/arrow-key navigation (independent of hardcoded indices) still work. Verified `grep -n 'cover-step'` returns nothing and slide/section counts are unchanged (14/14, 15 `data-slide` matches).

## 7. Walkthrough Verification

- [x] 7.1 Re-ran the structural check: `<section>`/`</section>` counts balanced (14/14), `data-slide` count is 15 (14 real slides + 1 in the JS selector string), all 8 `data-target` values (1,2,3,8,10,11,12,13) are within the valid 0–13 range, no CSS selector anywhere combines `.card` with `.proposal-*` (safe to have dropped `.card` from the six proposal items), and no other `accent-dot` references remain anywhere in prototype/.

## 9. Pitch Deck — Positioning Rewrite (drop 回診 from headline-weight statements)

Per design.md Decision 10: the live pitch never covers 回診 (appointment reminder), so the cover, Proposal, and Design Goal's one-sentence summary must not promise it. IA's screen inventory and demo-nav labels are untouched (they document real screens, not live-pitch scope); Persona/JTBD backup detail is left alone for now.

- [x] 9.1 Cover slide: replaced `<h1>吃藥不漏、回診不忘` with `<h1>記得清、看得懂、按得到`; updated the subtitle to drop "／回診提醒" → "...這款服藥提醒 App 該達成的設計目標。". The new headline is 2 characters longer than the original (11 vs 9, counting the ideographic commas) — since `clamp()` font-sizing tracks viewport width, not text length, removed `white-space:nowrap` from `.cover h1` so the first line wraps gracefully instead of risking overflow at medium viewport widths (addresses the Risk noted in design.md).
- [x] 9.2 Proposal slide (live-narrated): `proposal-meta`'s "服藥回診提醒 App" → "服藥提醒 App"; 問題描述 dropped "回診日期也記不清楚" and gained "，也說不清楚自己在吃什麼" instead; 成功指標's "清楚記得回診時間" → "看得懂在吃什麼"; matches the "Headline-weight statements do not claim scope the live pitch doesn't cover" spec scenario.
- [x] 9.3 Design Goal slide: `goal-statement` dropped "準時回診" and gained "記得清、看得懂、按得到" in its place, echoing the new cover tagline. Also fixed this same slide's `.slide-tag` ("服藥回診提醒 App" → "服藥提醒 App", directly above the goal-statement) — not explicitly listed in the task, but leaving it would have been an immediate, adjacent inconsistency on the same slide right next to the text just rewritten for the same reason; disclosing this small in-scope addition rather than silently expanding further (JTBD's separate slide-tag was left untouched, per design.md's explicit "left alone for now" scope).
- [x] 9.4 Verified via `grep -n '回診提醒\|編輯回診'`: the IA architecture slide's screen inventory (lines with `ia-box`) and the demo-nav menu's link labels (`impeccable-demo-nav__link`) still name 回診提醒/編輯回診 unchanged, exactly as intended per design.md Decision 10. JTBD's own `.slide-tag` ("服藥回診提醒 App") also still appears, matching the decision to leave JTBD's backup content alone for now.

## 10. Demo Rehearsal Script

Live 7-minute order: Proposal -> Evidence slide -> 3 live-narrated insight slides (language, recognition-scope, family-photo) -> demo. The 2 backup insight slides, Persona, JTBD, Design Goal, Design Guide, and IA slides stay in the deck as backup/Q&A material, not part of the live walkthrough.

- [ ] 10.1 Rehearse the demo sequence 08-voice-settings -> 01-today -> 02-confirmed -> 11-edit-med -> 05-family-overview end to end; verify each screen transition works via the existing bottom-nav/demo-nav links without dead ends.
- [ ] 10.2 In 08-voice-settings, switch to Taiwanese before moving on; verify the choice persists into 01-today (localStorage-backed per PRODUCT.md) so the voice differentiator is already active when 01 opens.
- [ ] 10.3 On 01-today, press the speak button to demonstrate the Taiwanese voice and point out the purpose caption/badge accent, then press "我吃過藥了"; verify this maps to the language-differentiation and recognition-scope slides already covered live.
- [ ] 10.4 On 02-confirmed, show the "按錯了可收回" undo path AND point out the family photo on the confirmation badge; verify this maps to the family-photo insight slide (task 3.3) — this is the one screen carrying two narration beats (error tolerance + the gamification-rejection insight).
- [ ] 10.5 On 11-edit-med, show the family setting a time slot's purpose caption and color accent; verify it visibly closes the loop with what was just shown on 01-today, proving recognition is a real family-to-elder workflow, not a static decoration.
- [ ] 10.6 On 05-family-overview, show today's progress summary and the exception banner; verify it maps to the family-exceptions-only backup slide's reasoning without needing that reasoning restated live (per design.md Decision 9, this insight is deliberately left as backup precisely because the demo alone already proves it).
- [ ] 10.7 Time the full rehearsal (3 insight slides + demo, including the extra family-photo narration beat on 02-confirmed) and confirm it fits within 7 minutes with a buffer; adjust pacing or trim a demo beat if it runs long.

## 11. Pitch Deck — Insight Slides Restructure (numbering, two-section split, stat cards)

Per design.md Decisions 11–12: unify the 5 insight slides' eyebrow tags to sequential numbers, split each into 洞察/設計決策 sections, and pull Insight 1's statistics into stat cards.

- [x] 11.1 Replaced all 5 insight slides' eyebrow tags ("洞察 Q1 · 研究與探索的洞察" / "洞察 Q2 · 核心維度的深化") with unified sequential numbers ("洞察 1" through "洞察 5"); removed the `.insight-question` element (the verbose original rubric-question small print) from all 5, since it existed specifically for Q1/Q2 traceability which no longer applies. Updated the pitch-deck-narrative spec's requirements accordingly (dropped all "tagged Q1"/"tagged Q2" language).
- [x] 11.2 Restructured all 5 insight slides into two `.insight-section` blocks each ("洞察" / "設計決策", using the `.panel-title` label style), wrapped in a new `.insight-sections` container (`flex:1` + `justify-content:space-evenly`, same technique as the Proposal sheet/Evidence blocks) so the two sections fill the slide evenly. This also resolved the unwanted `<br>` line breaks the user flagged (Insight 1's "...聽得懂中文語音。<br>於是決定..." and Insight 3's 3-sentence `<br>`-joined paragraph) — the halves now live in separate sections instead of one span with a manual break.
- [x] 11.3 Insight 1's "洞察" section: pulled the 3 statistics (65.9%, 28.5%, 96.57%) into individual stat cards reusing `.evidence-card`/`.evidence-stat`/`.evidence-label`, laid out 3-across via a new `.evidence-grid--cols-3` modifier (with a smaller stat font size than the Evidence slide's 2-across cards); kept a short `.qa-intro` paragraph below the cards explaining why the numbers matter (literacy ≠ spoken-language preference).
- [x] 11.4 Verified structure after the rewrite: `<section>`/`</section>` still balanced (14/14), `data-slide` still 15 matches (14 slides + 1 JS selector).

## 12. Pitch Deck — Remove the "阿福管家 ahok" Watermark

- [x] 12.1 Removed the 7 `<div class="slide-tag">阿福管家 ahok</div>` instances (Proposal, Evidence, and all 5 insight slides) via a single `replace_all` edit; left the other 4 `.slide-tag` instances untouched (JTBD, Design Goal, Design Guide, IA — each carries real per-slide content, not a repeated watermark). Verified `grep -n "slide-tag"` shows only those 4 remaining plus the CSS rule; `<section>`/`</section>` still balanced (14/14).
