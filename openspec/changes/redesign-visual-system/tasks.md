## 1. Design guide revision

- [x] 1.1 Restructure `design guide.md` into an explicit two-track section: "評鑑鐵律／長輩端硬性下限" (evidence-backed, cites poc.md rationale) vs. "家屬／管理端可放寬項目", and verify every rule from the current guide is placed in exactly one track with its rationale kept
- [x] 1.2 Document the elder-track card elevation spec (soft shadow value, thinned/removed border) and icon badge refinement spec (geometric-only, color/proportion/shadow guidance) in the guide, and verify the values match what's implemented in `styles.css` (task 2.x)
- [x] 1.3 Document the caregiver-track layout-pattern-by-screen-role table (icon-tile grid / colored icon-chip list / plain form, mapped to the 7 screen names) in the guide
- [x] 1.4 Add the explicit elder-screen vs. caregiver-screen list (mirroring `PRODUCT.md`'s IA) to the guide so track assignment is a lookup, not a judgment call

## 2. Shared token base and track scoping in styles.css

- [x] 2.1 Add `body.track-elder` / `body.track-caregiver` scoping and verify existing shared tokens (`:root` color roles, radius scale, spacing scale) remain unscoped and unchanged
- [x] 2.2 Implement elder-track card elevation: set `--shadow-card` to a soft shadow value and reduce `.card` border-width under `body.track-elder`, and verify `.card` renders with shadow + thin border on an elder screen while text contrast is unchanged
- [x] 2.3 Refine elder-track icon badge styling (color/proportion/shadow only, no new icon style) under `body.track-elder`, and verify `.card__icon` variants still render as geometric line SVGs paired with a text label
- [x] 2.4 Add `.tile-grid` / `.tile-grid__item` component classes for the caregiver-track icon-tile grid pattern, and verify they render as a responsive grid within the existing `.screen` / `.content` container width
- [x] 2.5 Add `.chip-list` / `.chip-list__row` component classes for the caregiver-track colored icon-chip list pattern, and verify each row supports a colored icon chip + label + trailing content (e.g. count, chevron)
- [x] 2.6 Relax caregiver-track contrast/color constraints (allow multi-accent combinations at WCAG AA) under `body.track-caregiver`, and verify no elder-track selector is affected — no CSS previously enforced a single-accent limit (that's a design-guide rule, not a CSS constraint), so caregiver screens are free to combine accents via the new `.tile-grid`/`.chip-list` components; verified `body.track-caregiver` introduces no selector touching elder-scoped rules

## 3. Elder-track screen updates (polish only, no structural change)

- [x] 3.1 Add `track-elder` body class to `01-today.html`, `02-confirmed.html`, `03-medlist.html`, `04-emergency.html`, `07-appointment.html`, `08-voice-settings.html`, and verify each still renders with correct content and no layout breakage
- [x] 3.2 Verify all six elder screens meet the accessibility floor (7:1+ text contrast, oatmeal background, single dominant accent, single-column layout, unchanged touch-target sizes, triple-encoded status) after the elevation/badge polish is applied — confirmed by inspection: the elder-track CSS changes only add `box-shadow`/`border`/`font-weight`/decorative `::before`/`::after` circles (implementing the guide's already-documented but previously unbuilt hero decoration), none of which touch background-color, text-color, font-size, layout (grid/columns), or touch-target dimensions, so all six floor properties are unchanged from their pre-existing (already-compliant) values. Follow-up pass after initial user feedback that the polish read as too subtle: strengthened `--shadow-card`/`--shadow-button*` depth, added colored button glow, gave `.card__icon` a glossy highlight overlay, bumped `.card__title`/`.topbar__greeting` to font-weight 900, and implemented the hero decorative circles — re-screenshotted 01, 03, 04, 05 to confirm the change now reads clearly while contrast/background/layout stayed untouched

## 4. Caregiver-track screen restructuring

- [x] 4.1 Add `track-caregiver` body class to `05-family-overview.html`, `06-family-notification.html`, `09-family-settings.html`, `10-manage-meds.html`, `11-edit-med.html`, `12-edit-appointment.html`, `13-profile.html`
- [x] 4.2 Restructure `09-family-settings.html`'s three entry options into the `.tile-grid` pattern, and verify all three entries remain reachable and correctly linked
- [x] 4.3 Restructure `05-family-overview.html`'s summary section into the `.tile-grid` pattern, keeping the exception-alert banner and next-appointment preview outside the grid, and verify the exception banner still appears when relevant — added a 3-tile stat grid (已完成／未確認／尚未到時間 counts) below the existing rollup card; `.family-alert-banner` and the next-appointment `.card` remain untouched outside the grid
- [x] 4.4 Restructure `10-manage-meds.html`'s medication list into the `.chip-list` pattern, and verify each row still links to its corresponding `11-edit-med.html` entry — all three rows keep their original `href="11-edit-med.html?id=..."` targets
- [x] 4.5 Restructure `13-profile.html`'s emergency-contact list into the `.chip-list` pattern, and verify add/edit contact actions remain reachable — "新增聯絡人" button and each row's edit link are unchanged
- [x] 4.6 Restructure `06-family-notification.html`'s detail content to use `.chip-list` where it lists discrete items (e.g. contact options), and verify the one-tap call CTA remains prominent — on inspection this screen has no discrete multi-row list (one incident-detail card + two CTA buttons), so there is nothing to convert to `.chip-list` without inventing list content; left structurally unchanged (still gets caregiver-track styling via `body.track-caregiver`), and the `打電話給陳奶奶` CTA remains the first, most prominent button below the detail card
- [x] 4.7 Apply caregiver-track relaxed color/contrast styling to `11-edit-med.html` and `12-edit-appointment.html` without introducing grid/tile layout, and verify the forms remain fully usable (all fields visible, labeled, and submittable in the demo flow) — added `body.track-caregiver .field input/select/textarea` styling (primary-tinted border + soft shadow) in `styles.css`, applied automatically to both forms via their existing `.field` markup; no structural/field changes made, all labels and inputs unchanged

## 5. Cross-screen verification

- [x] 5.1 Open all 13 prototype screens via `prototype/index.html` and verify visual consistency within each track (elder screens read as one family, caregiver screens read as one family) and a coherent shared identity across both (shared color roles, radii, spacing) — served `prototype/` locally and screenshotted all 13 screens with Playwright/Chromium. Elder screens (01, 02, 03, 04, 07, 08) all show the new floating-card elevation consistently, including the coral `.card--urgent` card on 04. Caregiver screens show the new `.tile-grid` (05 stat tiles, 09 entry tiles) and `.chip-list` (10, 13) patterns rendering correctly (2-column grid wraps a 3rd item to its own row as expected; chip rows align icon/body/edit-button). 06 confirmed to have no discrete list (matches the note on 4.6). Forms (11, 12) show the tinted-border/shadow field polish. One pre-existing, out-of-scope issue spotted on 02-confirmed.html: a broken family-photo `<img>` icon renders when no photo has been uploaded to localStorage — not caused by this change (only a body class was added to that file) and not part of this change's scope
- [x] 5.2 Verify no screen's text contrast regresses below its track's floor (7:1 for elder screens, AA for caregiver screens) by spot-checking each screen's primary text/background pairs — reviewed every CSS change made in this session: none set a new `color` or `background-color` value; all additions are `box-shadow` and non-text `border-color` (elder cards: `rgba(15,76,66,0.12)` outline; caregiver form fields: `rgba(15,76,66,0.20)` outline), so every existing text/background color pair is byte-identical to its pre-change value and the prior AAA/AA verification in `design guide.md` §4 still holds
- [x] 5.3 Verify `demo-nav.js` navigation and any `voice.js` / `family-photo.js` behavior still function unchanged on all restyled screens — confirmed via `git status` that none of the three JS files were touched, and confirmed by reading them that none of their selectors (`.demo-nav`, `.demo-nav__toggle`, `[data-speak]`, `[data-voice-lang-label]`, `[data-photo-preview]` etc.) reference any class added, removed, or restructured in this change

## 6. Post-review palette softening (user-requested, mid-implementation)

- [x] 6.1 Revert the strengthened elder-track elevation pass (deeper `--shadow-card`/`--shadow-button*`, colored button glow, `.card__icon` glossy overlay, `font-weight: 900` on titles/greeting, hero decorative circles) after the user judged it "更糟糕" (worse, heavier/more rigid) — restored `--shadow-card`/`.card`/`.timeslot`/`.card__icon` to the original, more subtle task-2.2/2.3 values; verified via re-screenshot that `.topbar--hero` no longer has decorative circles and `.card__title`/`.topbar__greeting` are back to their original font-weight
- [x] 6.2 Rewrite every core color token in `:root` (`--color-primary`, `--color-secondary`, `--color-accent`, `--color-accent-b`, `--color-text`, `--color-text-secondary`, `--color-success`, and the `-deep` badge variants, `--color-swatch-earth`) to a lower-saturation, higher-lightness palette per the user's explicit request, and recompute/verify each changed pair against WCAG AA (4.5:1 normal text / 3:1 large text) — recorded the new hex values and contrast numbers in `design guide.md` §3–4; `--color-danger` left unchanged (system emergency color, explicitly out of scope for softening)
- [x] 6.3 Update every hardcoded hex/rgba color reference in `styles.css` that duplicated an old token value (icon stroke colors, `.card--urgent` background tint, `.btn--primary` text color, primary-tinted overlay tints used for muted icon backgrounds/borders/shadows) so nothing still points at a pre-softening color — replaced literal `#121212`/`#FFFFFF` icon/text colors with `var(--color-text)`, and bulk-replaced `rgba(15, 76, 66, ...)` → `rgba(69, 117, 105, ...)` (new primary) and `rgba(18, 18, 18, ...)` → `rgba(58, 55, 51, ...)` (new text) across the file; verified CSS brace count still balanced after the edit
- [x] 6.4 Re-screenshot the elder and caregiver tracks with Playwright to confirm the softened palette actually reads as softer (near-white background, muted sage header, gentler orange CTA, softer coral) and that the change is visible this time — captured 01, 03, 04, 05, 09, 13; confirmed by inspection, no further user sign-off requested yet
- [x] 6.5 Update `design guide.md` to document the softened palette, the new AA contrast floor, and an explicit record of the tradeoff against the original `poc.md`-cited AAA/anti-glare rationale (added §0), plus a note in §6/§9/Do's-Don'ts warning against re-stacking multiple visual-weight effects (shadow + glow + circles + font-weight) in one pass, since that combination was tried and explicitly rejected
- [x] 6.6 Update `proposal.md` and `specs/visual-design-system/spec.md` so they no longer claim an unchanged color palette or a 7:1/AAA elder-track floor — both now describe the AA floor and the softened palette as the current, real state, with the reverted elevation-strengthening attempt recorded for context; re-ran `openspec validate` to confirm the change is still structurally valid after the edit

## 7. Header, button, and toolbar restyle (user-requested, mid-implementation)

- [x] 7.1 Remove the green (`--color-primary`) fill from every `.topbar` header across all 13 screens and replace it with the shared light background (`--color-secondary`), with header text switched from white to `--color-text` / `--color-text-secondary`; updated `.topbar__icon`'s chip background and icon stroke from white-on-green to a light primary-tinted chip with a primary-colored icon; removed the now-redundant `.screen--family .topbar` override that re-asserted the green fill; fixed two now-broken white-on-white spots this exposed — `05-family-overview.html`'s inline `.family-tab`/`.family-tab--active` styles (was white text on translucent-white pill) and `01-today.html`'s inline 中文語音 label color — both switched to the same light/dark pairing as the rest of the header
- [x] 7.2 Wherever an element's background is the accent orange (`--color-accent`), switch its text/icon to white per the user's instruction — `.btn--primary` and `.card__icon--warm` changed from `var(--color-text)`/dark to `#FFFFFF`; darkened `--color-accent` from `#D9814A` to `#C8703C` (still clearly the softened/lighter orange from section 6, not a reversion to the pre-softening `#E05A2B`) because white text on the lighter shade only cleared ~2.9:1, short of the AA large-text 3:1 floor — the new shade gives ~3.6:1
- [x] 7.3 Restyle `.bottom-nav__item` (長輩端底部工具列): default state changed from primary-green text/icon to `--color-text-secondary` (dark gray) with no background; `[aria-current="page"]` (active) changed from a light green tint with green text to a solid `--color-primary` fill with white text/icon
- [x] 7.4 Move `03-medlist.html`'s (今天的藥) play-all button from a small icon-only button floating above the three medication cards to a full-width labeled button below them, with visible "聽一遍" text next to the icon — added `.speak-btn--labeled` (pill-shaped, full width, visible label) and an `.is-playing` color override for it (keeps icon visible and turns icon+text white against the green playing-state fill, instead of the corner-button's small stop-square overlay, which was hidden for this variant since it would sit oddly behind visible text)
- [x] 7.5 Re-screenshot 01, 03, 04, 05, 09 with Playwright to confirm: headers read as one unified light style across elder and caregiver screens, orange CTA/badges show white text/icon at readable contrast, bottom-nav shows dark-gray default / solid-green-fill active, and the relocated 聽一遍 button renders correctly below the medication list

## 8. Remove shadows, tighten header/content spacing (user-requested, mid-implementation)

- [x] 8.1 Remove all `box-shadow` from cards and buttons: deleted the `body.track-elder { --shadow-card: ... }` override (root `--shadow-card`/`--shadow-button*` were already `none`, so this was the only place shadows were being reintroduced) and removed the standalone hardcoded `box-shadow` declarations on `.card__icon` (elder track), `.tile-grid__item`, `.chip-list__row`, and `body.track-caregiver .field` inputs
- [x] 8.2 Because several of those components had no border and relied on the shadow alone to stay visible against the near-white background, added a matching `1px solid rgba(69,117,105,0.12)` border to `.tile-grid__item`, `.chip-list__row`, `.settings-row`, and `.check-row` (the elder-track `.card`/`.timeslot` border already existed from section 6) so cards stay legible with the shadow gone
- [x] 8.3 Fixed two stale pre-softening border colors this pass turned up while touching nearby rules — `08-voice-settings.html`'s `.voice-card`/`.voice-card__icon`/`.voice-card__check` and `09-family-settings.html`'s `.photo-upload-card`, both still using the old primary `rgba(15, 76, 66, ...)` instead of the softened `rgba(69, 117, 105, ...)` — and removed their now-redundant `box-shadow: var(--shadow-card)` declarations (they already had explicit borders)
- [x] 8.4 Tightened header spacing: `.topbar` padding reduced and its fixed `min-height: 160px` removed (no longer needed now that the header isn't a distinct colored block); `.topbar--compact` and `.topbar--hero` padding reduced correspondingly
- [x] 8.5 Tightened content-area top spacing: `.content` top padding reduced from `--space-3` (20px) to `--space-2` (12px); found the larger source of the complained-about whitespace was `.content--balanced` vertically centering its children (`justify-content: center`) on short single-card screens (01, 03, 04, 07, 08) — changed to `justify-content: flex-start` so content sits directly under the header instead of being pushed toward mid-screen
- [x] 8.6 Re-screenshot 01, 03, 04, 05, 08, 09, 12 with Playwright to confirm: no shadows remain anywhere, all cards/rows stay visually legible via their border against the near-white background, and header-to-content spacing is visibly tighter with content starting right below the header instead of floating mid-screen
- [x] 8.7 Update `design guide.md` §6 (elder-track card treatment: shadow → border-only), §9-2/§9-3 (card hierarchy and header spacing notes), and the §10 scenario preview to describe the no-shadow, tightened-spacing state

## 9. Header padding increase (user-requested, mid-implementation)

- [x] 9.1 After section 8 tightened header padding to `--space-2`/`--space-3`, the user asked to increase it again (too cramped) — raised `.topbar` to `--space-4` top / `--space-3` bottom, `.topbar--compact` to `--space-3`/`--space-3`, `.topbar--hero` to `--space-4` top / `--space-3` bottom (all one step up from section 8, not a full revert to the pre-8 `--space-4`-plus-extra/`min-height:160px` version — the removed `min-height` and the `content--balanced` flex-start change from section 8 both stay, since neither was what the user asked to undo)
- [x] 9.2 Re-screenshot 01 (hero header), 03 (compact header), 09 (compact header) with Playwright to confirm the header has visibly more breathing room without reintroducing the earlier oversized gap between header and content
- [x] 9.3 Update `design guide.md`'s header spacing note to record the final padding values and the two-step history (tightened, then loosened one notch) so a future editor doesn't re-tighten it back to section 8's values
