# **高齡友善 APP 視覺設計指南 (Design Guide)**

**專案名稱**：高齡吃藥提醒與生活助手 APP

**目標使用者**：70 歲以上獨居長輩（Persona：陳奶奶，72 歲）＋ 45 歲照顧者（Persona：陳阿明）

**核心設計理念**：去醫療化、溫暖陪伴、極致無障礙（Accessibility）

## **零、2026 柔化改版說明（重要：對比度下限變更）**

這次改版，使用者明確要求把全部 13 個畫面的色彩系統一起降低飽和度、提高明度，讓整體從「暗沉僵硬」變成「柔和舒服」，並同意為此把對比度下限從 **WCAG AAA（7:1）放寬到 WCAG AA（一般文字 4.5:1／大型文字 3:1）**。這是一次刻意、知情的取捨，記錄如下，供之後任何人接手時理解為什麼數字變了：

* **變更範圍**：色彩 hex 值（主色、輔助色、強調色 A/B、文字色、成功/危險色、辨識色）、對比度下限，兩軌（長輩端／家屬端）一起調整，不分軌。
* **不變的部分**：色彩「角色」與分工邏輯不變（主色仍是頂色/標題、強調色 A 仍是主要 CTA、珊瑚色系仍是裝飾用重要標示……）；三重編碼、單一強調色、單欄大字、觸控區不縮小、避免藍紫色混淆等**非色彩數值本身**的規則維持不變（見第五節）。
* **與原始 poc.md 依據的衝突（刻意妥協，不是疏漏）**：第一節列的四項生理依據原本主張「對比敏感度下降 → 需 AAA 7:1」「眩光畏光 → 避免大面積純白背景」。這次改版兩者都做了退讓：對比度降到 AA，背景也從暖米白進一步淡化到接近純白。這個決定是使用者在看過柔化前後對照畫面後明確做的選擇，**不是自動採用**——如果之後要做真人可用性測試，這兩項退讓應該是優先驗證的項目。

## **一、高齡視覺生理特性與設計依據**

隨著年齡增長，高齡者的眼睛結構會發生顯著變異。本設計指南依據以下四項視覺退化特性制定：

1. **水晶體黃化（Lens Yellowing）**  
   * **生理現象**：眼睛水晶體隨年齡增長逐漸變黃、變暗，對藍光與青色光的吸收增加。  
   * **設計對策**：避免使用深藍、深紫作為核心資訊傳達色（易與黑色混淆）；提高暖色系（紅、橙、黃）的視覺權重。  
2. **對比敏感度下降（Loss of Contrast Sensitivity）**  
   * **生理現象**：對相近色相、灰階階層的辨識能力大幅降低。  
   * **設計對策**：不使用灰底灰字或淺色文字；對比度下限見第零節說明（2026 柔化改版已從 AAA 放寬到 AA）。  
3. **眩光與畏光感（Glare Sensitivity）**  
   * **生理現象**：高齡者易有白內障或瞳孔調節力變差，大面積純白背景在螢幕上會產生刺眼的眩光。  
   * **設計對策**：主背景原則上避免高亮度純白；2026 柔化改版已將背景大幅淡化到接近純白（見第零節說明的刻意妥協）。  
4. **色彩感知退化（Color Blindness & Deficiencies）**  
   * **生理現象**：單靠顏色無法 100% 精準傳達狀態（如紅綠燈狀態）。  
   * **設計對策**：採用「色彩 \+ 文字 \+ 圖示」三重編碼（Dual/Triple Encoding）。

這四項生理依據（poc.md 引用的社區研究亦支持長者對介面理解的障礙）原本是本指南「長輩端硬性下限」的唯一來源；2026 柔化改版對第 2、3 項做了刻意妥協（見第零節），第 1、4 項維持不變。

## **二、雙軌視覺系統總覽 (Two-Track System)**

**2026 視覺改版**：App 的 13 個畫面服務兩種視覺需求完全不同的使用者，因此版面骨架分成兩軌，共用同一套色彩角色、圓角尺度、間距尺度（見第三節），但執行規則不同：

* **長輩端（Elder Track）**：陳奶奶使用，6 個畫面。套用第五節「硬性下限」；第六節「執行精緻化」可持續打磨質感。
* **家屬／管理端（Caregiver Track）**：陳阿明使用，7 個畫面。無視覺退化限制，套用第七節「可放寬項目」與第八節「版面模式對照表」，可大膽向活潑精緻的參考風格靠攏。

**2026 柔化改版之後，色彩對比度下限兩軌已經一致（都是 AA）**，兩軌真正的差異只剩下版面骨架（單欄 vs 網格/清單）與強調色數量（單一 vs 多色並列），不再是對比度數字本身。

### 畫面對應表（與 PRODUCT.md 資訊架構一致）

| 軌道 | 畫面 | 檔案 |
| :---- | :---- | :---- |
| 長輩端 | 今日提醒首頁 | `01-today.html` |
| 長輩端 | 已完成確認 | `02-confirmed.html` |
| 長輩端 | 藥物列表 | `03-medlist.html` |
| 長輩端 | 緊急聯絡 | `04-emergency.html` |
| 長輩端 | 回診提醒 | `07-appointment.html` |
| 長輩端 | 語音設定 | `08-voice-settings.html` |
| 家屬／管理端 | 今日總結 | `05-family-overview.html` |
| 家屬／管理端 | 通知詳情 | `06-family-notification.html` |
| 家屬／管理端 | 管理設定入口 | `09-family-settings.html` |
| 家屬／管理端 | 吃藥管理列表 | `10-manage-meds.html` |
| 家屬／管理端 | 新增／編輯次數 | `11-edit-med.html` |
| 家屬／管理端 | 編輯回診 | `12-edit-appointment.html` |
| 家屬／管理端 | 個人資料設定 | `13-profile.html` |

一個畫面屬於哪個軌道，直接查這張表即可，不需要每次重新判斷。

## **三、色彩系統規範 (Color Palette Specification — 兩軌共用的 Token Base)**

色彩角色、圓角尺度（`--radius-card` / `--radius-badge` / `--radius-button` / `--radius-pill`）與間距尺度（`--space-1` 至 `--space-5`）是兩軌共用的 token base，不因軌道而異；差異只在於「怎麼用」（見第五、七節），不在於「有哪些顏色可用」。

### **1\. 品牌與主色調 (Primary & Secondary & Accent)**

**2026 柔化改版**：使用者要求全部 13 個畫面一起降低飽和度、提高明度，換取更柔和、不暗沉的視覺感受；對比度下限同步從 AAA 放寬到 AA（見第零節）。

| 色彩角色 | 色彩名稱 | Hex 色號 | RGB | 心理意像與功能應用 |
| :---- | :---- | :---- | :---- | :---- |
| **主色 (Primary)** | 柔霧綠 | \#457569 | rgb(69, 117, 105\) | 沉穩、健康、安全，但不再厚重。用於重要區塊邊框、圖示；**不再用於頂部導覽列大面積背景**（見第九節「頂部導覽列」新規則），**底部工具列 active 狀態也已改用橘色系**（見第九節「底部工具列」）。（原深青綠 \#0F4C42 提高明度、降低飽和度） |
| **輔助色 (Secondary)** | 近白暖調 | \#F8F6F2 | rgb(248, 246, 242\) | 溫暖、柔和，接近純白但保留一絲暖調，避免死白的生硬感。用於 APP 全域背景色、次要卡片底色、**頂部導覽列背景**（與全域背景一致，讓頭尾融為一體）。（原暖米白 \#FBF3E7 再淡化） |
| **強調色 A (Accent A)** | 柔陽橙 | \#C8703C | rgb(200, 112, 60\) | 醒目、溫暖、親切，但比原本柔和。用於主要行動按鈕（CTA）、重點色塊。**橘底一律搭配白字白圖示**（不再用深色文字）；比初版柔化（\#D9814A）再深一階，只為了讓白字達到 AA 大型文字門檻（3:1），不是走回頭路。小型徽章維持加深版 \#B85A2E 供更小尺寸文字使用。（原暖陽橙 \#E05A2B 降低飽和度） |
| **強調色 B (Accent B)** | 柔粉 | \#EA92A5 | rgb(234, 146, 165\) | 裝飾性「重要／緊急」大卡片專用（例如：緊急聯絡卡片、需要特別留意的用藥提示）。與柿紅（系統警告色）角色刻意分工：柿紅是系統自動判定的警示文字／圖示，柔粉是設計端主動標示的「這個比較重要」裝飾用色，兩者不可互相取代。使用限制見下方對比度章節。（原珊瑚粉 \#E8637A 提高明度） |

### **2\. 中性色與語意色 (Neutrals & Semantics)**

| 色彩角色 | 色彩名稱 | Hex 色號 | RGB | 心理意像與功能應用 |
| :---- | :---- | :---- | :---- | :---- |
| **主要文字** | 柔暖炭灰 | \#3A3733 | rgb(58, 55, 51\) | 高對比度文字，用於所有內文、時間、藥名。是「柔和的深色」，不是「淺灰」，不違反「嚴禁淺灰字」規則。（原沉墨黑 \#121212 提高明度、降低飽和度） |
| **輔助文字** | — | \#5A554E | rgb(90, 85, 78\) | 輔助說明文字下限。（原 \#333333 同步提高明度） |
| **卡片純白** | 高光白 | \#FFFFFF | rgb(255, 255, 255\) | 疊加於近白暖調背景上的卡片底色，靠陰影（非色差）分出卡片層次。僅限卡片範圍，不可作大面積背景以外的用途。 |
| **成功/完成** | 柔翡翠綠 | \#3D8069 | rgb(61, 128, 105\) | 用於「已吃藥」、「已預約回診」等成功確認狀態與大勾勾圖示。（原翡翠綠 \#2D7A5D 提高明度） |
| **警告/急件** | 柿紅 | \#D33F21 | rgb(211, 63, 33\) | 用於「多次未吃藥警告」、「緊急聯絡」按鈕等**系統判定**的警示文字／圖示，維持原色不柔化——緊急語意需要保留辨識度，不在這次柔化範圍內。 |
| **柔粉加深版** | 深柔粉 | \#C24D63 | rgb(194, 77, 99\) | 柔粉的加深版，供需要白字的小型徽章／按鈕使用（例如「重要」標籤），確保 AA 對比。 |
| **辨識色：柔焦糖棕** | — | \#A9895C | rgb(169, 137, 92\) | 家屬選填的辨識色配件，呼應實體藥盒既有配色，非狀態語意色。（原大地棕 \#8A6D3F 提高明度、降低飽和度） |

## **四、柔化改版對比度重新驗證 (Contrast Reference — AA 標準)**

以下數字是柔化後的色彩系統依據，兩軌共用同一份色彩：

* **主要文字 (\#3A3733) 在 卡片純白 (\#FFFFFF) 上**：對比度約 **11.7:1**（通過 AA，也通過 AAA）
* **主要文字 (\#3A3733) 在 近白暖調 (\#F8F6F2) 上**：對比度約 **11.0:1**（通過 AA，也通過 AAA）
* **主色柔霧綠 (\#457569) 在 近白暖調 (\#F8F6F2) 上**：對比度約 **4.4:1**（通過一般文字 AA 門檻；用於邊框、圖示，不再作頂部導覽列大面積背景或底部工具列 active 底色）
* **主要文字 (\#3A3733) 在 柔霧綠淡底 (rgba(69,117,105,0.10)) 上**（底部工具列 default 狀態改用 \#5A554E 輔助文字色）：輔助文字 \#5A554E 在近白暖調上對比度約 **6.8:1**（通過 AA，也通過 AAA），用於底部工具列未選取狀態的文字與圖示。
* **強調色 A 柔陽橙 (\#C8703C) 按鈕上的白字**：對比度約 **3.6:1**（按鈕文字 22pt 粗體、圖示徽章皆屬大型/非文字元件，通過 AA 大型文字與非文字對比門檻 3:1）。橘底一律搭配白字白圖示，不再用深色文字。小型徽章維持加深版 \#B85A2E \+ 白字（約 4.6:1，通過一般文字 AA），供更小尺寸文字使用。
* **強調色 B 柔粉 (\#EA92A5) 上的主要文字 (\#3A3733)**：僅通過大型文字 AA 門檻（3:1），一般段落文字仍改用近白暖調卡片＋柔霧綠邊框呈現。白字在柔粉上不合格，需白字小字時改用加深版**深柔粉 (\#C24D63)**，白字對比約 **4.6:1**（通過 AA）。
* **柔翡翠綠 (\#3D8069) 白字**：對比度約 **3.7:1**（通過大型文字/圖示門檻）；小字白字徽章改用加深版 \#2F6754，約 **6.6:1**（通過 AA）。

因為色彩系統本身已通過 AA 驗證，兩軌不需要另外重新測試對比度，只需遵守第五、七節的使用規則。

## **五、長輩端硬性下限 (Elder Track Hard Floor)**

適用範圍：`01-today.html`、`02-confirmed.html`、`03-medlist.html`、`04-emergency.html`、`07-appointment.html`、`08-voice-settings.html`（見第二節畫面對應表）。以下五條規則對應第一節的生理依據；**規則 1、2 已於 2026 柔化改版做出刻意妥協（見第零節），規則 3～5 未鬆動**：

| 規則 | 內容 | 生理依據 | 狀態 |
| :---- | :---- | :---- | :---- |
| 1. 核心文字對比 | 內文與標籤文字對背景至少 **4.5:1**（大型文字 3:1），WCAG AA | 對比敏感度下降 | 已從 AAA 7:1 放寬（刻意妥協） |
| 2. 背景色 | 全域背景使用近白暖調 (\#F8F6F2)，非死白但已非常接近純白 | 眩光與畏光感 | 已從暖米白進一步淡化（刻意妥協） |
| 3. 單一強調色 | 同一畫面最多一個「主導強調色」用於重點強調（成功/警示等固定語意色不算在內） | 色彩感知退化 | 未鬆動 |
| 4. 單欄大字、觸控區不縮小 | 單欄版面，不因改版縮小文字尺寸或觸控區域 | 對比敏感度下降＋一般高齡操作研究 | 未鬆動 |
| 5. 三重編碼 | 狀態一律「色彩＋文字＋圖示」三者同時呈現，不單靠顏色 | 色彩感知退化 | 未鬆動 |

## **六、長輩端執行精緻化 (Elder Track Polish)**

在不違反第五節前提下，長輩端的執行品質可以持續打磨：

* **卡片立體感（2026 再修訂：無陰影）**：使用者後續明確要求移除所有卡片與按鈕的陰影。資訊卡片一律「純色塊 \+ 1px 細邊框」界定範圍，不使用 `box-shadow`；`--shadow-card` 與 `--shadow-button*` 系列變數維持 `none`。這個決定同時解決了「近白背景 \+ 純白卡片」在無陰影時容易分不出卡片邊界的問題——細邊框（`rgba(69,117,105,0.12)`）是唯一的卡片範圍界定手法，不是陰影的替代裝飾，因此不可再拿掉邊框。這個改動**只調整卡片外框的視覺效果**，卡片底色與文字顏色不變，因此不影響第五節的對比度下限。
* **圖示徽章精緻化**：圖示徽章維持既有「16px 圓角方形」幾何線條 SVG（不可改成插畫或吉祥物風格），同樣不使用陰影或玻璃感高光疊層。圖示徽章一律搭配文字狀態標籤，不單獨承擔語意（呼應第五節規則 5）。
* **裝飾圓、字重、間距節奏**：Hero 頭部裝飾圓、字重層級、卡片間距如果要再加強，須先跟使用者確認方向，不要在同一輪一次疊加太多視覺效果——這正是上一輪被打回票的原因。

## **七、家屬／管理端可放寬項目 (Caregiver Track Freedoms)**

適用範圍：`05-family-overview.html`、`06-family-notification.html`、`09-family-settings.html`、`10-manage-meds.html`、`11-edit-med.html`、`12-edit-appointment.html`、`13-profile.html`（見第二節畫面對應表）。陳阿明（45 歲，無視覺退化）不受第五節限制，此軌道可以：

* 使用第四節驗證過的 AA 對比即可——但仍必須沿用第三節的色彩角色，不新增色彩。
* 同一畫面同時使用多個強調色（例如柔陽橙、柔粉、柔翡翠綠並列），呈現活潑的多色卡片感。
* 使用比長輩端更高的資訊密度（例如小卡並列、次要文字縮小），只要仍符合 AA 對比。

## **八、家屬／管理端版面模式對照表 (Layout Pattern by Screen Role)**

家屬／管理端的版面骨架依「畫面性質」決定，是查表對應、不是每個畫面重新設計：

| 版面模式 | 用途 | 適用畫面 |
| :---- | :---- | :---- |
| **icon-tile 網格** | 入口／儀表板類：一眼看懂有哪些選項 | `09-family-settings.html`（管理設定入口三個選項）、`05-family-overview.html`（今日總結上半彙總區） |
| **彩色 icon chip 清單** | 清單／紀錄類：需要完整資訊、可掃描 | `10-manage-meds.html`（吃藥管理列表）、`13-profile.html`（緊急聯絡人清單） |
| **純表單** | 資料輸入類：維持乾淨不花俏，不強行套網格 | `11-edit-med.html`、`12-edit-appointment.html` |

`06-family-notification.html` 是單一事件詳情頁（一張事件卡片＋兩個 CTA 按鈕），沒有可轉換的多筆清單內容，不套用 chip-list，維持既有結構＋家屬端配色即可。

`05-family-overview.html` 的例外提醒橫幅與下次回診預覽維持在網格區塊之外，不納入 tile 網格。

## **九、UI 元件應用規範與設計法則**

### **1\. 核心操作按鈕 (CTA Buttons)**

* **未完成/請吃藥**：  
  * 底色：\#C8703C (柔陽橙)  
  * 文字：\#FFFFFF (白字粗體 22pt)  
  * 附加圖示：大圖示（如：藥丸/手掌點擊），白色線條圖示  
* **已完成確認**：  
  * 底色：\#3D8069 (柔翡翠綠)  
  * 文字：\#FFFFFF (白字粗體 22pt)  
  * 附加圖示：✓ 大勾勾與文字「早上藥已吃」  
* **緊急／重要行動**（例如緊急聯絡卡片內的強調用途）：  
  * 底色：\#C24D63 (深柔粉，即柔粉加深版)  
  * 文字：\#FFFFFF (白字粗體 22pt)  
  * 與柿紅 \#D33F21 的系統警告按鈕（如「119」）用途不同：深柔粉用於「設計端主動強調」，柿紅用於「系統判定的警示」。

### **2\. 卡片層次架構 (Card Hierarchy)**

* **APP 底色**：全螢幕使用 \#F8F6F2 (近白暖調)。  
* **資訊卡片 (Information Cards)**：24px 圓角，一律不用陰影，靠 1px 細邊框（`rgba(69,117,105,0.12)`）界定卡片範圍（見第六節）；家屬／管理端可依第七、八節彈性調整，但同樣不使用陰影。  
* **圖示徽章 (Icon Badge)**：清單／卡片中用來識別藥物或項目的圖示容器，**2026 再改版：正圓形實心色塊**（從圓角方形改回正圓形，仍是實心彩色，不是空心描邊），色彩語意不變（暖色＝待處理、翡翠綠＝已完成、中性＝尚未到時間、焦糖棕＝家屬自訂辨識色）；圖示本身為裝飾用途，一律搭配文字狀態標籤，不單獨承擔語意。長輩端的精緻化規則見第六節。  
* **重要程度差異化**：清單中「需要留意」或「下一個要吃」的項目，卡片留白與圖示徽章尺寸會比一般項目略大，已完成項目則略為收斂並降低不透明度，藉此在維持單欄、大字體、不縮小資訊密度的前提下，呈現「重要的卡片看起來比較大」的視覺層次，而不是改用會壓縮觸控區域或文字大小的多欄網格（此規則限長輩端；家屬／管理端的版面模式見第八節）。  
* **卡片內資訊分層（2026 新增）**：一張藥物卡片同時包含「這是什麼時段/狀態」「藥名」「用途」「怎麼吃」四種不同角色的資訊時，**不可全部用同一種灰字樣式堆疊**，一律依角色分開處理：
  - 白話用途說明（如「顧心臟、顧血壓的藥」）改用 `.purpose-chip` 小標籤，顏色呼應該筆藥物圖示徽章的辨識色（`--warm`/`--success`/`--muted`/`--earth`/`--urgent` 對應變體），讓「這是什麼用途」一眼可辨，不必逐字閱讀。清單類畫面（如 `03-medlist.html`）用這個標籤呈現用途，不使用灰字。
  - 服用方式／叮嚀文字（如「飯後配溫水吃…」）跟藥物識別資訊視覺上分開呈現，不要塞進同一個資訊區塊裡跟藥名/用途擠在一起。
* **今日提醒首頁的焦點卡片（`01-today.html` 專用，2026 新增，經多輪精修，以下是目前定案的狀態）**：這個畫面只有單一個「現在該吃的藥」卡片，是全 App 資訊密度最低、最聚焦的一張畫面，因此採用跟其他清單型畫面不同的專屬版型（呼應使用者提供的參考圖，先是計時器 App 的圓形進度環構圖，後改為習慣追蹤 App 的圓角矩形卡片構圖）：
  - 整個內容區塊（標題列＋卡片＋提醒＋按鈕）垂直置中於畫面（`.content--center`），並整體再往上偏移一點（`.med-hero { margin-top: -24px; }`）。
  - **卡片本體**（`.med-card`）是正方形的圓角矩形／squircle（`aspect-ratio: 1` 維持正方形、100px 圓角），白底、1px 細邊框、無陰影。**播放按鈕跟「圖示＋文字」主要內容是兩個各自獨立定位的區塊，不共用同一個 flex 排版**：
    - `.med-card__main`（圖示＋標題＋用途標籤）用 `position: absolute; top/left: 50%; transform: translate(-50%,-50%)` 真正置中在卡片正中央。
    - `.med-card__speak`（播放按鈕）用 `position: absolute; left: 50%; bottom: 16px; transform: translateX(-50%)` 獨立釘在卡片下緣附近，離下緣只有 16px（先前用過 44px，實測發現按鈕離卡片下緣還有 45px 空隙、離上方文字卻只有 20px 左右，跟使用者要的「靠近下緣、跟文字拉開距離」正好相反，改成 16px 之後兩個距離才對調過來）。左右可點擊範圍另外加大到 `padding: 0 40px`（原本 20px）——底色是 `none`，加大 padding 不會讓按鈕看起來變寬，純粹擴大觸控熱區，方便長輩點擊，不影響版面視覺。
    - **播放中的視覺回饋（2026 新增，後調整過三次）**：播放時卡片外圈有一圈淺黃色 ring 往外擴散淡出再重來（`@keyframes med-card-ring`，`1.6s ease-in-out infinite`，從貼齊卡片、45% 不透明度，擴大到 1.28 倍、完全透明；呼吸頻率調過兩次，中間曾統一成 2.4s 跟卡片同步，卡片動畫拿掉之後，ring 自己的頻率又再調快回 1.6s）。用 `:has()` 選擇器偵測卡片內的播放按鈕有沒有 `.is-playing`（`.med-card-wrap:has(.med-card__speak.is-playing)::before`），**不需要修改 `voice.js`**——`.is-playing` 本來就是按鈕自己在切換的 class，`:has()` 讓外層 ring 直接感知這個狀態，不用讓共用的 `voice.js` 去認識這個畫面特有的 DOM 結構。ring 的實作沿用先前「wrap＋`::before`＋z-index 疊在卡片後方」的技術（見上面「疊在卡片後方的圓形」筆記），只是這次外框改成會動的擴散動畫，不是靜態光暈。
    - **卡片本身不做呼吸縮放**：一開始卡片跟 ring 各自有自己的呼吸/擴散動畫，兩個動畫週期一度沒對齊（1.2s vs 1.6s）造成視覺上不同步、看起來混亂，統一成同一個 2.4s 週期後解決了不同步的問題；但使用者後來進一步要求卡片本身完全不要動，只保留 ring 的呼吸感——`.med-card` 上的 `:has()` 呼吸動畫（`med-card-pulse`）跟對應的 `@keyframes` 已經整個移除，只剩 `.med-card-wrap::before` 的 ring 動畫。**如果之後又要幫卡片加回動畫效果，切記要跟 ring 的週期（目前 2.4s）保持一致，這條規則本身沒有變。**
    - **技術筆記（為什麼要拆成兩個獨立定位區塊）**：一開始播放按鈕跟主要內容共用同一個 flex column，不管是置中還是靠下對齊，只要按鈕存在，「圖示＋文字」這組主要內容的視覺重心就會被按鈕往上推；而且只要調整按鈕自己的 `margin-top` 想把它往下移，等於在同一個 flex 裡增加內容總高度，反而會把上面的圖示／文字一起往上擠壓，兩個目標互相打架，怎麼調都顧此失彼。拆成兩個獨立的絕對定位區塊之後，「圖示＋文字」永遠精準置中，播放按鈕的位置只由自己的 `bottom` 值決定，兩者互不影響，之後如果任何一邊要再微調位置，直接調對應的距離值即可。
    - 圖示徽章（`.med-card__icon`，外框背景 108px 不變）在最上方；裡面的 SVG 圖示改成純色填滿樣式（`fill`，不用線條 `stroke`），圖示本身縮小到 40px。徽章底色是明確的**黃色**（`#F5B942`，不是偏棕的土黃色），圖示是**白色**填色＋黃色描邊分隔線（跟其他色系圖示徽章的白圖示語彙一致）。**已知的無障礙妥協**：白色圖示在這個亮度的黃色底上，非文字對比只有約 2.2:1，低於 WCAG 1.4.11 的 3:1 下限——原本改用深色圖示是為了解決這個對比落差，但使用者明確要求維持白色（深色圖示視覺上「看起來像奇怪的黑色髒污」），所以這裡是刻意接受對比不足的裝飾性圖示，跟本指南其餘「一律驗證過對比」的原則不同。之所以能接受，是因為圖示本身純裝飾、旁邊一定有文字標籤重複語意（三重編碼規則仍成立，只是圖示本身這一層對比沒有達標）。**技術筆記**：一開始把整顆膠囊填滿白色、拿掉中間那條分隔線，結果變成一個看不出是藥物的橢圓形——分隔線才是「這是膠囊」的關鍵視覺線索，不是裝飾。修法是把分隔線加回來，用跟徽章底色一樣的顏色（黃色）當作疊在膠囊填色上的描邊，做出經典的「雙色膠囊」圖示效果。
    - 「早上的藥　3 顆」（`.med-card__title`）**同一行**，藥名在前，32px、500 字重。
    - 用途標籤（`.med-card .purpose-chip`）**拿掉底色**，文字維持橘色（用加深版 `--color-accent-deep` 而不是一般的 `--color-accent`，因為文字直接印在白卡片上，需要 4.5:1 以上的一般文字對比，加深版約 4.6:1 剛好通過，一般橘色只有約 3.6:1 不夠）。
    - **播放按鈕**（`.med-card__speak`）拿掉底色（含播放中的綠底提示）。不再是純圖示按鈕，圖示旁邊加了文字標籤，圖示也真的换成不同圖形（不是同一個圖示疊圖）——預設顯示喇叭圖示＋「播放」文字，播放中切換成暫停圖示（兩條直線）＋「暫停」文字（兩組 `<svg>`／`<span>` 都寫在 DOM 裡，用 `.is-playing` 切換 `display`，不是動態換圖示內容或改文字）；圖示與文字皆為深灰色（`var(--color-text-secondary)`）；圖示 26px、文字 18px。
  - 服用叮嚀文字在卡片外的純文字（`.med-hero__reminder`，置中，無邊框、無圖示，`margin-top: var(--space-2)`），字級 16px、字重 500（比一般內文 19px 略小，但字重比 400 稍重，讀起來不會太輕）——這是這張卡片裡唯一比第五節單欄大字基準更小的文字，因為它是次要的叮嚀說明，不是需要辨識的核心數字/名稱。
  - 主色系是橘色系（`--color-accent`／`card__icon--warm`／`.purpose-chip--warm`）；紅色系（`--color-danger`）只保留給 119 等系統警示語意使用，這張卡片本身不是警示情境。
  - 主要 CTA 按鈕（`.med-hero__cta`）文字精簡為「我吃過藥了」，全圓角藥丸形（`.btn--pill`），`min-height: 62px`（略高於長輩端硬性下限 60dp，留一點餘裕），打勾圖示是跟 `status-tag--done` 一致的裸打勾圖示，不帶外框圓圈。
  - **技術筆記（同一種選擇器優先權陷阱，目前已知踩過四次，是這個畫面最大的排查重點）**：這個畫面反覆出現「改了數值、視覺上卻毫無變化」的狀況，四次原因都一樣——新加的 class 跟既有的共用 class（`.text-body`／`.btn`／`.card__icon`／`.speak-btn`）優先權相同（都是單一 class），CSS 檔案裡共用 class 定義在後面，後蓋前，等於白改：
    1. `.med-hero__reminder` 蓋不過 `.text-body`（字級改了沒生效）。
    2. `.med-hero__cta` 蓋不過 `.btn`（`min-height` 改了沒生效）。
    3. `.med-card__icon` 蓋不過 `.card__icon`（icon 尺寸改了沒生效，是使用者反映「icon 看起來幾乎一樣」的真正原因）。
    4. `.med-card__speak` 蓋不過 `.speak-btn`（底色拿掉指令沒生效，播放按鈕移進卡片後截圖檢查時自己發現的，這次在使用者看到之前就先抓出來修掉了）。
    5. `.speak-btn.is-playing svg { opacity: 0; }`（給純圖示播放按鈕用的，播放中把圖示淡出、露出後面的停止方塊疊圖）在暫停圖示上仍然生效，即使 `display:block` 已經蓋過對應的 `display:none`——**這不是同一種「後蓋前」的優先權問題，是兩條規則分別控制不同的 CSS 屬性（`display` vs `opacity`），彼此不衝突、會同時套用**，結果就是暫停圖示雖然有 `display:block` 但因為 `opacity:0` 還在，視覺上完全看不見。修法是額外補上 `opacity: 1`，跟 `display: block` 寫在同一條規則裡。
    6. `.speak-btn:active { transform: scale(0.9); }`（給按下時的縮放回饋動畫用的）直接蓋掉了這顆按鈕原本靠 `transform: translateX(-50%)` 做水平置中的效果——**跟第 5 點是同一種「分屬不同規則但作用在同一個 CSS 屬性」的陷阱，只是這次兩條規則剛好都設定了同一個 `transform` 屬性，而 `transform` 不會合併多條規則的值，後生效的整個取代前面的**，按下瞬間置中偏移量消失，按鈕視覺上跳動一下。修法是把兩個 transform 效果合併寫在同一條、優先權更高的規則裡（`transform: translateX(-50%) scale(0.9)`）。
    統一修法：只要新樣式要蓋掉一個共用元件 class，選擇器一律寫成「兩個 class 疊加」（例如 `.card__icon.med-card__icon`、`.speak-btn.med-card__speak`），不要單獨用新 class 名稱，才能確保優先權不被檔案順序影響；**但如果共用規則跟新規則設定的是同一個屬性（尤其是 `transform`、`opacity` 這種不會自動合併的屬性），光靠選擇器優先權贏過還不夠，必須把兩邊要的效果寫進同一條規則裡，否則優先權雖然贏了、其中一邊的效果還是會整個消失。**這個畫面下次再出現「改了視覺卻毫無變化」或「兩個效果只剩一個」，先檢查是選擇器優先權問題還是同屬性互相取代問題，不要懷疑瀏覽器快取或改錯檔案；每次新增卡片內元件的樣式後，最好都先自己截圖／模擬互動確認一次再回報。
  - **另一個技術筆記（跟優先權無關，是 flex 縮寬問題）**：`.med-hero` 曾經沒有明確寬度，身為 `.content--center`（`align-items: center`）底下的 flex 子項會用 fit-content 縮到剛好包住內容，這時候底下子元素的 `width:100%` 只會滿版於 `.med-hero` 那個被縮小的寬度，不會滿版於整個畫面（實測寬度只有 315px，不是預期的 440px）。修法是在 `.med-hero` 明確加上 `width: 100%`，讓它先撐滿 `.content`。之後任何「XX 滿版」的需求，如果外層容器有 `align-items: center`，都要先檢查容器本身有沒有 `width: 100%`。
  - 移除了次要的「今天不想吃這顆？跟阿明說一聲」連結——緊急聯絡入口已經有底部工具列跟獨立的「緊急聯絡」畫面可以到達，這個畫面只保留最核心的單一動作（確認吃藥），降低選擇負擔。
  - 這個版型是這個畫面獨有的，不是要推廣到其他清單型畫面；`03-medlist.html` 等多筆項目的畫面繼續用第八節的清單版型，不套用這個焦點卡片。
* **Hero 頭部區塊**：首頁與家屬今日總結等「歡迎時刻」畫面，頂部導覽列跟其他畫面一樣使用淺色底（見下方「頂部導覽列」），暫不加裝飾圓——上一輪加入裝飾圓＋深陰影＋加重字重的組合被使用者認為讓畫面更沉重，已復原；之後若要再嘗試裝飾圓，應該單獨小幅度嘗試並確認觀感，不要跟其他視覺加重效果一起疊加。

### **3\. 頂部導覽列 (Header／Topbar)**

* **統一淺色背景**：所有畫面的頂部導覽列（包含長輩端與家屬端）一律使用近白暖調 (\#F8F6F2) 背景，不再使用柔霧綠大面積色塊——這是使用者明確要求的變更，讓頁首跟頁面主體視覺融為一體，不再有「深色色塊」的分區感。
* **文字與圖示**：標題文字改用主要文字色 (\#3A3733)（2026 再修訂：字級從 24px 加大到 26px），日期文字用輔助文字色 (\#5A554E)；右上角圖示按鈕（如語音設定齒輪）改用淺灰底 (rgba(58,55,51,0.08)) 圓形色塊＋主要文字色 (\#3A3733) 線條圖示（2026 再修訂：原本是柔霧綠淡底＋柔霧綠圖示，後來改成中性灰底＋深色圖示，讓右上角這顆按鈕更安靜，不搶頭部主標題的注意力）。
* **家屬端分頁標籤（如今日總結／歷史紀錄）**：套用第四項「底部工具列」同一套邏輯——未選取為柔霧綠淡底＋輔助文字色，選取中為柔霧綠實心底＋白字。
* **留白節奏（2026 再修訂三次）**：頂部導覽列不再需要用大量留白撐出「色塊」的視覺份量（因為背景已經跟頁面融為一體），因此拿掉了固定 160px 的最小高度；但完全收到最緊（`--space-2`）後使用者又覺得太侷促，上下 padding 定案為 `.topbar`／`.topbar--hero` 用 `--space-4`／`--space-3`，`.topbar--compact` 用 `--space-3`／`--space-3`。**左右 padding** 後續又從 `--space-3`（20px）加大到 `--space-4`（28px），`.topbar`／`.topbar--compact` 都套用，比 `.content` 自己的左右 padding（仍是 `--space-3`）更寬，讓標題文字離螢幕邊緣更遠一點。`.content` 的頂部留白從 `--space-3`（20px）收斂到 `--space-2`（12px）維持不變；單卡片畫面常用的 `.content--balanced` 不再垂直置中內容（`justify-content: flex-start`），避免卡片被推到畫面中間、頭尾之間留下大片空白，這個改動也維持不變（`01-today.html` 是例外，改用 `.content--center` 讓焦點內容垂直置中，見第九節「今日提醒首頁的焦點卡片」）。

### **4\. 底部工具列 (Bottom Nav／Toolbar)**

* **預設狀態**：圖示與文字一律使用輔助文字色 (\#5A554E)（深灰階、非柔霧綠），無底色。
* **選取中狀態（active／aria-current，2026 再修訂數次，以下為目前定案）**：淺橘底 (rgba(200,112,60,0.05)，刻意調得很淡) ＋深橘圖示與文字 (`--color-accent-deep`)。這條規則改過三次：一開始是「柔霧綠實心底＋白字」的色塊寫法；後來改成「柔霧綠純文字/圖示變色、無底色」；最後定案是橘色系（跟卡片系統的暖色語言更一致）、有底色但非常淡，藉此在「有底色標示當前位置」跟「不要變成搶眼色塊」之間取得平衡。

### **5\. 設計 Do's & Don'ts**

#### **✅ 正確做法 (Do)**

1. **三重狀態編碼**（長輩端硬性下限）：使用顏色的同時，必須加上大字體文字與圖示（如：「綠色 \+ ✓ \+ 已完成」、「橙色 \+ 圖示 \+ 未吃藥」）。  
2. **大面積色塊點擊區**（長輩端硬性下限）：按鈕高度至少需要 60dp 以上，邊框清晰，方便長輩手指點擊。  
3. **主要文字用柔和深色，不是淺灰**：主要文字一律採用 \#3A3733，輔助說明文字最低限制為 \#5A554E，嚴格禁止使用 \#999999 或 \#CCCCCC 等淺灰字——「柔化」是指顏色本身變柔和、不飽和，不是把文字變淡變難讀。  
4. **強調色的文字色要驗證過**（兩軌共用）：柔陽橙 (\#C8703C) 背景一律搭配白字白圖示（大型文字/圖示約 3.6:1，通過 AA）；小型徽章改用加深版 (\#B85A2E) \+ 白字（約 4.6:1）；柔粉 (\#EA92A5) 只用於大型粗體標題或圖示徽章，小字內文改用深柔粉 (\#C24D63) \+ 白字。

#### **❌ 嚴禁做法 (Don't)**

1. **長輩端嚴禁使用淺灰色文字**：長輩會完全看不見灰字說明。柔化改的是色相飽和度與明度，不是把文字變成灰階。  
2. **兩軌皆避免藍紫色混淆區**：切勿使用藍紫色作為關鍵狀態提示（長輩水晶體黃化後不易分辨深藍與黑色）。  
3. **長輩端禁止僅依靠顏色區分狀態**：色弱或視力模糊長輩無法分辨純紅與純綠的差異。  
4. **兩軌皆嚴禁把柔粉當白字按鈕底色**：基礎柔粉 (\#EA92A5) 對白字對比不足，需白字時一律改用加深版 \#C24D63。  
5. **兩軌皆嚴禁把柔粉與柿紅混用**：柔粉是設計端主動標示的裝飾色，柿紅是系統判定的警示色，兩者角色不同，不可互相取代。  
6. **不要在同一輪一次疊加多個視覺加重效果**：深陰影＋光暈＋裝飾圓＋加重字重同時做，容易讓「柔和」的訴求適得其反變成「用力過猛」。每次只調一兩個變數，做完就給使用者看，再決定要不要繼續加。

## **十、陳奶奶情境實例應用預覽**

* **畫面狀態：早上 8:00 提醒吃藥**  
  * **背景／頂部導覽**：都是近白暖調 (\#F8F6F2)，頭尾融為一體，顯示大字：「陳奶奶，早安！」（柔暖炭灰 \#3A3733）  
  * **中央卡片**：高光白底色 (\#FFFFFF)，24px 圓角，無陰影、僅細邊框（見第六節），柔暖炭灰大字 (\#3A3733) 寫著：「早上的藥（3顆）」，旁邊搭配圓角方形圖示徽章  
  * **底部按鈕**：柔陽橙色 (\#C8703C) 超大按鈕、白色文字＋白色圖示，寫著：「按這裡：我吃過藥了」  
  * **底部工具列**：「每日提醒」項目為選取中狀態，圖示與文字變柔霧綠、無底色；其餘三項為預設狀態，輔助文字色圖示＋文字，同樣無底色

* **畫面狀態：緊急聯絡**  
  * **重要提示卡片**：柔粉淡底 (\#EA92A5 18% 透明度) \+ 深柔粉邊框 (\#C24D63)，搭配柔粉圖示徽章，內文維持標準 \#3A3733 深色字，用來標示「這是特別需要被看到的畫面」  
  * **119 按鈕**：仍維持柿紅色 (\#D33F21)，因為這是系統既定的緊急救護語意，不受柔粉裝飾色與整體柔化改版影響
